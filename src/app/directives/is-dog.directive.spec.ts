import { IsDogDirective } from './is-dog.directive';

describe('IsDogDirective', () => {
  it('should create an instance', () => {
    const directive = new IsDogDirective();
    expect(directive).toBeTruthy();
  });
});
