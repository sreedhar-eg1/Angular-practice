import { UniqueRoletempDirective } from './unique-roletemp.directive';

describe('UniqueRoletempDirective', () => {
  it('should create an instance', () => {
    const directive = new UniqueRoletempDirective();
    expect(directive).toBeTruthy();
  });
});
