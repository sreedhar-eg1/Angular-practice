import { FlyingHerosPipe } from './flying-heros.pipe';

describe('FlyingHerosPipe', () => {
  it('create an instance', () => {
    const pipe = new FlyingHerosPipe();
    expect(pipe).toBeTruthy();
  });
});
